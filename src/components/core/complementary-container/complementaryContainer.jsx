import React from 'react';
import PropTypes from 'prop-types';

const ComplementaryContainer = props => {
  return (
    <div>ComplementaryContainer</div>
  )
};

ComplementaryContainer.propTypes = {};

export default ComplementaryContainer;